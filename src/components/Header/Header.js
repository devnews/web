import React from 'react';
import styles from './Header.css';

const Header = () => {
    return (
        <div>
            <div className={styles.logo}>
                devnews
            </div>
        </div>
    )
};

export default Header;
